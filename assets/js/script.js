

const grid = document/querySelector('.grid')

// grid to be used for the game

function createBoard() {
    for (let i = 0; i < cardArray.length; i++) {
        var card = dcoument.createElement('img')
        card.setAttribute('src', 'images/logo.jpeg')
    }
}